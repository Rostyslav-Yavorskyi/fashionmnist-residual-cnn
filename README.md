## Setup

### Option 1: pip
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### Option 2: uv (faster)
```bash
uv venv
source .venv/bin/activate
uv pip install -r requirements.txt
```
